import { StyledHeader } from "../styles/shared.styles";

type Props = {};

const Contacts = () => {
    return (
        <StyledHeader>
            <h1>Contacts</h1>
            <h2>Hit me up</h2>
        </StyledHeader>
    );
};

export default Contacts;
